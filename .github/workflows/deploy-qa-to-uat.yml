name: Deploy QA → UAT

on:
  push:
    branches:
      - uat

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: UAT
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Instalar Salesforce CLI
        run: npm install sfdx-cli --global

      - name: Autenticar na UAT
        run: |
          echo "${{ secrets.SFDX_AUTH_URL_UAT }}" > authfile.txt
          sfdx force:auth:sfdxurl:store -f authfile.txt -a UATPlayground

      - name: Deploy real
        run: |
          sfdx force:source:deploy -p force-app -u UATPlayground --testlevel RunLocalTests --json